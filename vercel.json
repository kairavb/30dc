{
    "builds": [
        {"src":"app.py", "use":"@vercel/python", "engines": {"node": "18.x"}, "config": { "maxLambdaSize": "15mb", "runtime": "python3.8" }}
    ],
    "routes": [
        {"src":"/(.*)", "dest":"app.py"}
    ]
}
